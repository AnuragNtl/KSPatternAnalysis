#ifndef FORMATS_H
#define FORMATS_H
#include<iostream>
#include<fstream>
using namespace std;
class Convert
{
  protected:
    virtual void convert(ostream &out);
  public:
    Convert(int )
};
#endif

